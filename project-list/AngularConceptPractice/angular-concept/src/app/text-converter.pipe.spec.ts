import { TextConverterPipe } from './text-converter.pipe';

describe('TextConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TextConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
